# Rule reference

Generated by `threatdiff rules docs`. 88 rules across 10 packs.

## Spoofing

| Rule | Severity | Confidence | Triggers on | Question |
| --- | --- | --- | --- | --- |
| `authn.default-or-hardcoded-credentials` | critical | medium | added | Does this credential exist in any environment that is reachable from outside a laptop? |
| `authn.jwt-verification-disabled` | critical | high | added | Can a caller now present a token they minted themselves and be believed? |
| `crypto.tls-verification-disabled` | critical | high | added | Which connection stops being authenticated, and who sits on that network path? |
| `audit.webhook-or-callback-added` | high | medium | added | How does this endpoint know the request really came from the provider? |
| `authn.mfa-or-verification-bypass` | high | medium | any | Under which conditions can the second factor now be skipped, and can a caller reach those conditions on their own? |
| `authn.session-cookie-flags-weakened` | high | high | added | Which of the three cookie protections is lost, and what does that enable? |
| `control.csrf-protection-removed` | high | high | any | Can this endpoint now be triggered by a cross-site request that carries the victim's session cookie? |
| `crypto.insecure-randomness` | high | medium | added | Does an attacker who observes a few of these values gain the ability to predict the next one? |
| `authn.password-comparison-not-constant-time` | medium | medium | added | Is this comparison timing-safe, and is it comparing a hash rather than a raw secret? |
| `disclosure.open-redirect` | medium | medium | added | Can the destination be an absolute URL on a domain you do not control? |

## Tampering

| Rule | Severity | Confidence | Triggers on | Question |
| --- | --- | --- | --- | --- |
| `crypto.signature-check-skipped` | critical | medium | added | Is the boolean or exception from this verification actually acted upon? |
| `injection.dynamic-code-evaluation` | critical | high | added | Where does the evaluated string come from, and can any of it be attacker controlled? |
| `injection.os-command` | critical | medium | added | Can any part of this command line be influenced by data that crossed a trust boundary? |
| `injection.sql-string-building` | critical | medium | added | Is every value in this statement bound as a parameter, or is any of it concatenated from caller-controlled input? |
| `injection.unsafe-deserialization` | critical | high | added | Does the serialized data ever originate outside your trust boundary, including from a cache, a queue or a file upload? |
| `control.input-validation-removed` | high | low | removed | What now reaches the parser, the database or the template that previously could not? |
| `crypto.weak-hash-for-security` | high | low | added | Is this hash used for a security decision, or only as a non-security identifier such as a cache key? |
| `data.deletion-without-safeguards` | high | low | added | What is the blast radius if the filter above this line is empty or null? |
| `injection.nosql-operator` | high | medium | added | Could the caller supply an object where you expected a string, turning the value into a query operator? |
| `injection.server-side-template` | high | medium | added | Is the template itself built from input, or only the values passed into it? |
| `injection.xss-raw-html-sink` | high | medium | added | Is the value written here already sanitised, and by a library rather than a hand-rolled escape? |
| `ci.pipeline-modified` | medium | high | file | Does this pipeline have access to production credentials, and does the change alter when or with whose code it runs? |
| `control.security-header-removed` | medium | medium | removed | Which browser-side protection is lost, and is any user-controlled content rendered by the affected responses? |
| `injection.ldap-or-header` | medium | medium | added | Are control characters (parentheses for LDAP, CR/LF for headers) removed from the interpolated value? |

## Repudiation

| Rule | Severity | Confidence | Triggers on | Question |
| --- | --- | --- | --- | --- |
| `audit.log-level-or-retention-reduced` | medium | medium | added | Will you still be able to reconstruct an incident that is discovered a month after it happens? |
| `control.audit-log-removed` | medium | low | removed | If this action is abused in production, what evidence remains that it happened and who did it? |
| `control.security-test-removed` | medium | medium | removed | Was the behaviour this test guarded also removed, or is the guarantee now untested? |
| `audit.error-swallowed` | low | low | added | Is the swallowed error one that indicates a security control failed? |

## Information disclosure

| Rule | Severity | Confidence | Triggers on | Question |
| --- | --- | --- | --- | --- |
| `crypto.hardcoded-key-iv-or-salt` | critical | medium | added | Where does this material come from at deploy time, and how is it rotated? |
| `crypto.password-hashed-with-fast-digest` | critical | medium | added | What does an offline attacker with the database do with these hashes? |
| `iac.storage-made-public` | critical | high | added | What is in this bucket now, and what will someone put in it in six months? |
| `control.encryption-removed` | high | medium | removed | What data is now written or transmitted in a form that a database dump or log export would reveal? |
| `crypto.kdf-parameters-lowered` | high | low | added | What is the resulting verification time, and was the reduction driven by a measurement or by a timeout? |
| `crypto.weak-cipher-or-mode` | high | medium | added | Is this required by an external protocol you cannot change, or a default that nobody revisited? |
| `data.bulk-export-or-admin-endpoint` | high | low | added | Who can call this, is it rate limited, and does every use leave an audit record naming the operator? |
| `disclosure.cors-permissive` | high | medium | added | Which origins are actually allowed, and does the configuration also send credentials? |
| `disclosure.debug-mode-enabled` | high | high | added | Can this configuration reach a deployed environment? |
| `disclosure.sensitive-field-added-to-response` | high | low | added | Does every consumer of this response need every field, and does any of it belong to another user? |
| `iac.encryption-or-logging-disabled` | high | medium | added | Is this resource holding regulated or customer data, and what does the control being switched off protect against? |
| `injection.path-traversal` | high | low | added | Can the input escape the intended directory with `..`, an absolute path, or a symlink? |
| `injection.ssrf-request-from-input` | high | low | added | Can the caller point this request at your internal network, cloud metadata service, or localhost? |
| `injection.xxe-external-entities` | high | medium | added | Does this parser process DOCTYPE declarations and external entities from documents you did not author? |
| `secret.logged-or-printed` | high | medium | added | Where do these logs end up, and who can read them? |
| `data.migration-touches-sensitive-column` | medium | medium | added | Is this migration reversible, and does it change who can read which rows? |
| `data.pii-field-introduced` | medium | low | added | Is this field covered by your data inventory, retention policy and deletion flow? |
| `disclosure.directory-listing-or-static-root` | medium | medium | added | What else is inside the directory being served? |
| `disclosure.error-details-to-client` | medium | medium | added | Does the returned message reveal file paths, SQL, library versions or internal hostnames? |
| `disclosure.sensitive-data-in-url` | medium | low | added | Where will this URL be recorded - proxy logs, browser history, the Referer header of the next request? |
| `disclosure.source-map-or-artifact-shipped` | low | medium | added | Is this build going to production, and does the source contain comments, internal endpoints or credentials? |

## Denial of service

| Rule | Severity | Confidence | Triggers on | Question |
| --- | --- | --- | --- | --- |
| `dos.archive-extraction` | high | medium | added | Can an entry in this archive escape the target directory, or expand to far more than it claims? |
| `control.rate-limit-removed` | medium | medium | removed | Which endpoint loses its request ceiling, and what does an attacker gain by calling it in a loop? |
| `dos.no-timeout-on-outbound-call` | medium | low | added | What happens to this worker if the remote side accepts the connection and then never responds? |
| `dos.regex-from-input-or-catastrophic` | medium | low | added | Can a caller control either the pattern or a long input matched against it? |
| `dos.resource-limits-removed` | medium | medium | removed | Which workload can now consume the whole node, and what else runs on it? |
| `dos.unbounded-request-body-read` | medium | medium | added | What is the largest body a caller can send here, and what happens to memory when they do? |
| `dos.retry-without-backoff` | low | low | added | If the dependency is down, does this loop make the outage worse? |

## Elevation of privilege

| Rule | Severity | Confidence | Triggers on | Question |
| --- | --- | --- | --- | --- |
| `authz.tenant-scope-dropped-from-query` | critical | low | removed | Does this query still return only rows belonging to the caller's tenant? |
| `ci.pull-request-target-with-head-checkout` | critical | high | added | Does this workflow both run with repository secrets and execute code from the pull request branch? |
| `container.privileged-or-host-namespace` | critical | high | added | Which specific capability is needed, and is a container escape from this workload equivalent to node compromise? |
| `control.auth-middleware-removed` | critical | medium | removed | Is every route that this middleware protected still covered by an equivalent check after the change? |
| `control.authz-annotation-removed` | critical | high | removed | Which endpoints or handlers stop requiring authorization because of this change, and is that intentional? |
| `authn.auth-bypass-conditional` | high | low | added | Can the condition that skips the check be reached in production? |
| `authz.new-endpoint-without-auth` | high | low | added | Who is allowed to call this endpoint, and where is that decision made? |
| `authz.object-lookup-from-request-id` | high | low | added | Does the query constrain the object to the caller's tenant, account or ownership, or only to the identifier they supplied? |
| `authz.wildcard-permission-grant` | high | medium | added | Which specific capabilities does this wildcard actually need, and what else does it now include? |
| `ci.self-hosted-runner-introduced` | high | high | added | Is this runner ephemeral, and can a fork's pull request schedule a job on it? |
| `control.authz-policy-file-deleted` | high | high | file | Which decisions did this policy make, and what is the default outcome now that it is gone? |
| `control.feature-flag-security-default-flipped` | high | low | added | Is this default shipped to production, and who can flip it at runtime? |
| `iac.iam-wildcard-policy` | high | medium | added | Which specific actions does this principal need, and what does the wildcard additionally grant? |
| `iac.network-open-to-world` | high | high | added | Which port and protocol does this open, and is the service behind it designed to face the internet? |
| `container.runs-as-root` | medium | medium | any | Does anything in this image need UID 0 at runtime, or only at build time? |

## Supply chain

| Rule | Severity | Confidence | Triggers on | Question |
| --- | --- | --- | --- | --- |
| `ci.remote-script-piped-to-shell` | high | high | added | What is at the other end of that URL today, and what will be there during your next deploy? |
| `supplychain.dependency-from-vcs-or-url` | high | high | added | Is this reference immutable, and who can change what it points at? |
| `supplychain.install-lifecycle-script` | high | high | added | What does this script do, and does it need to run on every developer machine and every CI job? |
| `supplychain.integrity-check-weakened` | high | high | added | What check is being turned off, and what problem was it blocking? |
| `supplychain.lockfile-deleted` | high | high | file | What resolves your dependency versions now, and is the result reproducible? |
| `supplychain.registry-or-mirror-changed` | high | high | added | Who operates this registry, and does adding it let a public package shadow an internal one? |
| `ci.action-not-pinned-to-digest` | medium | high | added | If the owner of this action force-pushes the tag tomorrow, what runs in your pipeline? |
| `supplychain.base-image-unpinned` | medium | high | added | Which exact image will this build pull next month? |
| `supplychain.vendored-binary-added` | medium | medium | file | Where did this binary come from, and can anyone reproduce it from source? |
| `supplychain.dependency-manifest-changed` | low | high | file | For each new direct dependency: who maintains it, how many downloads does it have, and when was it last released? |

## Secrets exposure

| Rule | Severity | Confidence | Triggers on | Question |
| --- | --- | --- | --- | --- |
| `secret.cloud-provider-credential` | critical | high | added | What can this identity do, and has it been disabled? |
| `secret.private-key-block` | critical | high | added | Which systems trust this key, and has it been revoked yet? |
| `secret.service-token` | critical | high | added | Which account does this token belong to, what scopes does it carry, and has it been revoked? |
| `ci.secret-exposed-to-step` | high | medium | added | Can the value of a secret reach a log, an artifact, or a string that gets evaluated by the shell? |
| `secret.connection-string-with-password` | high | high | added | Is this a real environment or a local compose file? |
| `secret.env-file-added` | high | medium | file | Does this file contain real values, and should it be in .gitignore instead? |
| `secret.generic-credential-assignment` | high | low | added | Is this a real credential, or a variable name, URL fragment or placeholder? |

