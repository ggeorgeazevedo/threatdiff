module github.com/ggeorgeazevedo/threatdiff

go 1.22

// threatdiff is intentionally dependency-free.
// An AppSec tool that runs inside your CI should not be the largest
// supply-chain surface in your pipeline. Everything below is stdlib.
